The resource files have to be in "res" directory of your project.

Looks like:

TicTacToe (Project Directory)
  build
  dist
  src
    res (<- create this directory in src and put in there the image files)
  
Or modify the LoadImages() Method in the source code
